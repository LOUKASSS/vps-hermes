# Opérateur

GitHub `LOUKASSS`, git `loukass7@pm.me`. Un seul agent Hermes, compétences via skills.

Préfère le français, réponses courtes, faits vérifiés. Ne pas supprimer vault / projets / Postgres sans confirmation.

Déploiement VPS via git puis `sudo command-center …` (lancé par l'opérateur, depuis SSH, Orca ou herdr), jamais depuis le conteneur agent. Projets : `/srv/workspace`.

Santé → Postgres schéma `health` et `/opt/data/private/health.sqlite3`. Marchés → schéma `markets` et `/opt/data/private/markets.sqlite3`. Pas dans MEMORY.md.
