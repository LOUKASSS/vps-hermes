.env
.env.*
!.env.example
*.local
*.local-test
acme.json
.claude/
.maintenance
.update-hold
*.zip
restore/
hermes-ssh/

# never: bot tokens pasted next to the scripts (rotate them if this ever matched)
discord.txt
*.token
*.tokens
# runtime state hermes writes into a profile's skills/ (profiles.sh export excludes it too)
profiles/*/skills/.curator_state
profiles/*/skills/.curator_ledger.jsonl
profiles/*/skills/.locks/
profiles/*/mcp/**/node_modules/
profiles/*/mcp/**/dist/
profiles/*/mcp/**/.built-from
